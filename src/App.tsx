export default function App() {
  return (
    <div style={{ maxWidth: "var(--container-max)", margin: "0 auto", padding: "var(--space-7)" }}>
      <h1 style={{ fontSize: "var(--font-size-7)", lineHeight: "var(--line-7)", margin: 0 }}>
        Home (Tokens Wired)
      </h1>
      <p style={{ color: "var(--color-text-muted)", fontSize: "var(--font-size-3)", lineHeight: "var(--line-3)" }}>
        If this looks clean (dark background, nice type), Phase 2 is working.
      </p>
      <div style={{
        marginTop: "var(--space-6)",
        padding: "var(--space-6)",
        background: "var(--color-surface)",
        border: "1px solid var(--color-border)",
        borderRadius: "var(--radius-3)",
        boxShadow: "var(--shadow-2)"
      }}>
        Surface card example - SAQIB

        
      </div>
    </div>
  );
}